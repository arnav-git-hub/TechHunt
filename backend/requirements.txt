# TechHunt backend dependencies
# Versions below are pinned to what was validated on the CI environment.
# asyncpg requires a C compiler (or pre-built wheel from PyPI on Linux/Mac).

# -------------------------------------------------------------------
# Web framework
# -------------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# -------------------------------------------------------------------
# Database
# -------------------------------------------------------------------
sqlalchemy>=2.0.30
asyncpg>=0.29.0     # requires libpq / C compiler — pre-built wheels on PyPI
alembic>=1.13.0

# -------------------------------------------------------------------
# Configuration
# -------------------------------------------------------------------
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.2.0

# -------------------------------------------------------------------
# Security (future auth — scaffolded now)
# -------------------------------------------------------------------
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0

# -------------------------------------------------------------------
# HTTP client (for connector health checks and future live connectors)
# -------------------------------------------------------------------
httpx>=0.27.0

# -------------------------------------------------------------------
# Testing
# -------------------------------------------------------------------
pytest>=8.2.0
pytest-asyncio>=0.23.0
anyio>=4.4.0
